---
name: motion-react
description: Use when animating React slides with motion/react (Framer Motion). The reliable subset, the potholes (layoutId, AnimatePresence, resize replays), and recipes. Canonical copy: .agents/skills/motion-react/SKILL.md
---

See `.agents/skills/motion-react/SKILL.md` for the full guide — the reliable
motion/react subset, the potholes (shared-layout morphs need `AnimatePresence`;
`layout` replays on resize; element-type swaps remount; entrances tied to
`isActive` replay), and copy-paste recipes (staged reveal, accordion, one-shot
entrance, shared-element morph, SVG particle burst).
